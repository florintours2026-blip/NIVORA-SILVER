{
  "name": "nivora-silver-backend",
  "version": "2.0.0",
  "private": true,
  "description": "NIVORA SILVER ecommerce MVP with Firebase catalog, customer sourcing, protected product URL importer and optional GA4.",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js && find src -name \"*.js\" -print0 | xargs -0 -n1 node --check"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "morgan": "^1.10.0"
  }
}
