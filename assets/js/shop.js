window.pageInit=()=>{const g=document.querySelector("#shopGrid"),s=document.querySelector("#search"),sort=document.querySelector("#sort");let cat=new URLSearchParams(location.search).get("category")||"all";
function render(){let a=state.products.filter(p=>cat==="all"||p.category===cat),q=(s.value||"").toLowerCase();if(q)a=a.filter(p=>(p.name_ar+" "+p.name_en).toLowerCase().includes(q));if(sort.value==="low")a.sort((x,y)=>x.price-y.price);if(sort.value==="high")a.sort((x,y)=>y.price-x.price);g.innerHTML=a.length?a.map(productCard).join(""):`<div class="empty" style="grid-column:1/-1">${tr("empty")}</div>`}
document.querySelectorAll(".chip").forEach(b=>b.onclick=()=>{document.querySelectorAll(".chip").forEach(x=>x.classList.remove("active"));b.classList.add("active");cat=b.dataset.cat;render()});s.oninput=render;sort.onchange=render;document.querySelectorAll(".chip").forEach(b=>b.classList.toggle("active",b.dataset.cat===cat));render()}
