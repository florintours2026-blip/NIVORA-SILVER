/* NIVORA legacy compatibility file.
   The active storefront uses assets/js/site.js.
   Kept valid so accidental inclusion cannot break page parsing. */
(function(){
  'use strict';
  window.NivoraLegacyMain = true;
})();
