# NIVORA SILVER — HTML
46 HTML pages: 28 storefront + 18 admin.
These are structured starter pages and are designed to reference the CSS/JS tables already created.
The production version will populate each page dynamically from the API/database.
